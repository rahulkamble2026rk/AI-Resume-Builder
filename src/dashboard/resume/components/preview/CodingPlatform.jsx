/* eslint-disable react/prop-types */
/* eslint-disable no-unused-vars */
/*import React from 'react'

function CodingPlatform({ resumeInfo }) {
    console.log('resumeInfo:', resumeInfo); // Debugging log

    return (
        <div className='my-6'>
            <h2 className='text-center font-bold text-sm mb-2'
                style={{
                    color: resumeInfo?.themeColor
                }}>
                Coding Platforms
            </h2>
            <hr style={{
                borderColor: resumeInfo?.themeColor
            }} />
            <div className='grid grid-cols-2 gap-3 my-4'>
                {resumeInfo.ids.map((platform, index) => (
                    <div key={index} className='flex items-center'>
                        <a href={platform.url} target="_blank" rel="noopener noreferrer" className='text-xs text-blue-500 underline'>
                            {platform.name}
                        </a>
                    </div>
                ))}
            </div>
        </div>
    )
}

export default CodingPlatform*/
